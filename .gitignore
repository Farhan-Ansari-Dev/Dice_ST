# Terraform-generated files – do NOT commit them
*.tfstate
*.tfstate.backup
tfplan
*.tfplan
.terraform/
# IDE / OS files
.DS_Store
*.log

# ── Terraform ─────────────────────────────────────
# Do not commit the local .terraform directory (provider binaries, state files, etc.)
.terraform/
**/.terraform/
*.tfstate
*.tfstate.backup
tfplan
*.tfplan
# ── Terraform ─────────────────────────────────────
# Do not commit the local .terraform directory (provider binaries, state files, etc.)
.terraform/
**/.terraform/
*.tfstate
*.tfstate.backup
tfplan
*.tfplan
